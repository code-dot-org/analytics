version: 2

models: 
  - name: dim_course_structure
    description: dim table for courses data
    columns: 
      - name: course_id
        description: unique id for table
        tests:
          - unique
          - not_null

  - name: dim_levels
    description: dim table for levels data
    columns: 
      - name: level_id
        description: ""
        tests: 
          - unique
          - not_null

  # - name: dim_sections
  #   description: migration of run_section_log; dim table for sections
  #   columns: 
  #     - name: section_id
  #       description: ""
  #       tests: 
  #         - unique
  #         - not_null